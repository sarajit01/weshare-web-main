import { BrandLogoDirective } from './brand-logo.directive';

describe('BrandLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandLogoDirective();
    expect(directive).toBeTruthy();
  });
});
