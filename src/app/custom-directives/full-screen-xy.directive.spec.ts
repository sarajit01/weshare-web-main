import { FullScreenXYDirective } from './full-screen-xy.directive';

describe('FullScreenXYDirective', () => {
  it('should create an instance', () => {
    const directive = new FullScreenXYDirective();
    expect(directive).toBeTruthy();
  });
});
