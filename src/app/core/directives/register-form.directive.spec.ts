import { RegisterFormDirective } from './register-form.directive';

describe('RegisterFormDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterFormDirective();
    expect(directive).toBeTruthy();
  });
});
