import { SearchBusinessPipe } from './search-business.pipe';

describe('SearchBusinessPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBusinessPipe();
    expect(pipe).toBeTruthy();
  });
});
