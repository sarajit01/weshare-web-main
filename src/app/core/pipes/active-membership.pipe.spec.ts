import { ActiveMembershipPipe } from './active-membership.pipe';

describe('ActiveMembershipPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveMembershipPipe();
    expect(pipe).toBeTruthy();
  });
});
